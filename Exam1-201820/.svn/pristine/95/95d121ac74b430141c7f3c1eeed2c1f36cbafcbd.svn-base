
public class TestThisClass {

	/**
	 * Craziness calculator. 
	 * 
	 * Returns true if a student has at least 3 assignments due within the next day.
	 * Otherwise returns false: 
	 * One exception: if there is only one day until break, they have hope
	 * regardless of how many assignments are due, 
	 * so aren't crazy - returns false.
	 *
	 * This code has a bug. One of your tests should expose it.
	 * 
	 */
	public static boolean isGoingCrazy(int numAssignmentsDue, int numDaysUntilBreak) {
		if (numDaysUntilBreak <= 1) {
			return false;
		}
		
		if (numAssignmentsDue > 3) {
			return true;
		}
		return false;
	
	
	}
}
